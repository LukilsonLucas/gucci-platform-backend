// backend/database/database.js

// Este arquivo agora é um placeholder ou pode ser removido,
// pois o Firestore será inicializado via firebaseConfig.js.
// Não precisamos mais do sqlite3 aqui.

// Exporta um objeto vazio para evitar erros de referência em outros arquivos
// que possam estar tentando importar 'db' deste local.
module.exports = {};

// Nota: As tabelas e a lógica de inicialização do SQLite foram removidas
// pois estamos migrando para o Firestore.
